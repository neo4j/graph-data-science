apply plugin: 'java-library'

description = 'Neo4j Graph Data Science :: Projection'

group = 'org.neo4j.gds'

dependencies {
    compileOnly(neo4j.common) { transitive = false }

    implementation project(':config-api')
    implementation project(':core')
}
